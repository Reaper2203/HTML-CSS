let fruits = ["banana", "apple", "orange", "mango"];

fruits = fruits.sort(); //sorting alphabetical order
fruits = fruits.sort().reverse(); //sorting reversely

for(let fruit of fruits){
    console.log(fruit);
}
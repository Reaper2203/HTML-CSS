//method chaining = calling one method after another in one continues line of code

let userName = "renz";

let letter = userName.charAt(0).toUpperCase().trim();
//selecting the character then making it uppercase
//making a cleaner code by combining two methods by a dot

console.log(letter);